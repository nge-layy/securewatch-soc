# images/

Exported diagram images (PNG/SVG) referenced from the docs.

The primary versions of all diagrams in this project are maintained as Mermaid code directly inside each phase's Markdown files (rendered natively by GitHub), so this folder is used for any diagram exports that benefit from a static image — for example, versions embedded in external write-ups or a resume-linked portfolio page.
